import PointForCoordinate from './PointForCoordinate';

// You need to export your test here to enable it.
// The exported names need to correspond with the names in
// `example/ios/AirMapsExplorerTests/AirMapsExplorerTests.m`.
export default {
  PointForCoordinate,
};
